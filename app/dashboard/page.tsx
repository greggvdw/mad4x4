'use client';import { useEffect,useState } from 'react';import { supabaseBrowser } from '../../lib/supabase';export default function Dashboard(){const [user,setUser]=useState<any>(null);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser().auth.getUser();setUser(data?.user??null)})()},[]);if(!user) return <div><p>Please log in to view the dashboard.</p></div>;return (<div><h1>Member Dashboard</h1><div className="grid" style={{marginTop:'1rem'}}><div className="card"><h3>My Membership</h3><p>Membership status and renewal (Stripe).</p></div><div className="card"><h3>My Events</h3><p>View your registrations and upcoming trips.</p></div><div className="card"><h3>Documents</h3><p>Members-only minutes and resources.</p></div></div></div>);}