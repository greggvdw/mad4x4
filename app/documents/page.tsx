import { supabaseServer } from '../../lib/supabase';export default async function Documents(){const s=supabaseServer();const {data:docs}=await s.from('documents').select('*').order('uploaded_at',{ascending:false}).limit(100);return (<div><h1>Documents</h1><div className="grid" style={{marginTop:'1rem'}}>{(docs||[]).map((d:any)=>(<div key={d.id} className="card"><h3>{d.title}</h3><p>Visibility: {d.visibility}</p><a className="btn" href={d.url} target="_blank" rel="noreferrer">Open</a></div>))}</div></div>);}